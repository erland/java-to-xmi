# Stereotypes and annotations

This project supports **type-level Java annotations** by representing them as UML stereotypes.

## Overview

1. During UML build, the tool creates a UML Profile named `JavaAnnotations`.
2. ...